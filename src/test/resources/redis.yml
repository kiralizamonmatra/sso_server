spring:
  redis:
    host: localhost
    port: 8850
  data:
    redis:
      client-type: lettuce # lettuce or jedis
      database: 0
      host: localhost
      port: 6379
      password: remora3672
      lettuce:
        pool:
          max-active: 16
          max-idle: 8
